from http use { Server, Mux }
type entrypoint = const func ([string]) -> (int)
entrypoint main :: ([string] args) int {
    Server app = new Server{
        port :: 8000,
    }
    app.addMux% '/host/', new Mux {
        handle :: (Request req, ResponseWriter res) void | res.write("Hello world")
    }
    server.listen()
    return 1;
}

main% []